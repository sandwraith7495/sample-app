<%= form_for current_user.active_relationships.build, remote: true do |form| %>
  <div><%= hidden_field_tag :followed_id, user.id %></div>
  <%= form.submit t("follow"), class: "btn btn-primary" %>
<% end %>
